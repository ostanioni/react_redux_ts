import { DefaultTheme } from 'styled-components'

export const theme: DefaultTheme = {
  borderRadius: '5px',
  bgColor: 'white',
  textColor: 'white',
  selectBorderColor: 'black',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  }
}
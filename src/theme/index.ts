import { DefaultTheme } from 'styled-components'

export const theme: DefaultTheme = {
  borderRadius: '5px',
  bgColor: 'red',
  textColor: 'white',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  }
}
import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  borderRadius: '5px',
  bgColor: 'red',
  textColor: 'white',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },
};

export { theme };